{{>Header}}

<div class="main-content">
	{{#is-mainpage}}
		{{>Homepage}}
		{{>subjects-and-countries}}
		{{>article_items}}
	{{/is-mainpage}}
	{{#category_page}}
		{{>category}}
	{{/category_page}}
	{{#country_page}}
		{{>country}}
	{{/country_page}}
	{{#article_page}}
		{{>article}}
	{{/article_page}}
	{{#other_page}}
	    <div class="container">
	        <div class="row">
		       {{{html-body-content}}}
            </div>
        </div>
	{{/other_page}}
</div>
{{>Footer}}
